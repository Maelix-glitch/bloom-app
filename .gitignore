# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.shots/

# local environment (never commit real keys)
.env
.env.local
.wrangler
.tanstack

# Supabase CLI session state — holds the project ref, org id and the DB pooler
# host. It is local scaffolding, not source, and has no business in the repo.
supabase/.temp/

# delivery kits are published as release assets, not committed
docs/*/
!docs/cleanup/
