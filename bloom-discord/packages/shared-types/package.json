{
  "name": "@bloom/shared-types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Zero-dependency vocabulary shared by every Bloom bot: ids, error codes, domain enums, Result.",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean"
  }
}
