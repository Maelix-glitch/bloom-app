# Bloom — Supabase connection.
# Copy this to .env.local and fill it in. Without these the app still works:
# every page falls back to device-local storage and says so.

VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR_PUBLISHABLE_OR_ANON_KEY
